task: "pt-9"
intent: "landing conflict fixture"
profile: "standard"
scope:
  in: ["code"]
  out: []
use_cases:
  - id: "UC-1"
    actor: "Owner"
    preconditions: []
    steps: ["apply patch to main"]
    postconditions: ["patch lands cleanly"]
behavior_contracts: []
acceptance:
  tests: ["true"]
  manual: []
policy:
  network: "deny"
  allow_domains: []
gates:
  required: ["pre_smoke","audit","adversarial_review","validate","land","post_smoke"]
